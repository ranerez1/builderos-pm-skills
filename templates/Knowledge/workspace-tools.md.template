# Workspace Tools

> First time? Run `/00-onboarding` in Claude Code to fill this file conversationally.

Skills read this file to resolve tool URLs/IDs and MCP server names.
**Update this file when you switch tools or workspaces — no skill files need changing.**

---

## Backlog
- **Tool**: _e.g. Jira / Linear / GitHub Issues / Notion_
- **Workspace/Project URL**: `<paste link>`
- **Board/Project ID (optional)**: `<id-or-empty>`
- **MCP Server**: `<mcp-server-name>`
- **Notes**: if your tool doesn’t have a “board”, use the closest concept (project/team/repo) and leave board-specific fields blank.

## Release Board
- **Tool**: _e.g. Notion / Jira / Linear / Google Sheet_
- **Workspace/Project URL**: `<paste link>`
- **Board/Project ID (optional)**: `<id-or-empty>`
- **MCP Server**: `<mcp-server-name>`

## Customer Meeting Transcripts
- **Tool**: _e.g. Notion / Google Drive / Otter / Linear_
- **Workspace/Project URL**: `<paste link>`
- **Board/Project ID (optional)**: `<id-or-empty>`
- **MCP Server**: `<mcp-server-name>`
- **Local CSV export paths (optional)**: `<one or more absolute/relative paths to .csv files>`
- **Local docs/folder paths (optional)**: `<one or more paths to folders/files with transcripts/notes>`
- **Other customer-feedback sources (optional)**: `<describe extra sources + where/how to access them>`

## Support Tickets
- **Tool**: _e.g. Intercom / Zendesk / Linear / Jira_
- **Workspace/Project URL**: `<paste link>`
- **Board/Project ID (optional)**: `<id-or-empty>`
- **MCP Server**: `<mcp-server-name>`

## Analytics
- **Tool**: _e.g. Mixpanel / Amplitude / PostHog_
- **MCP Server**: `<mcp-server-name>`

## Attachments (optional)
- **How to attach files to tracker items**: `<describe the configured method, e.g. MCP tool / CLI helper / manual>`
- **Required environment variables (if any)**: `<list-or-empty>`

---

## Knowledge Sources

Used by `/12-ingest-knowledge` to pull external docs and partition them into `Knowledge/02–06` as token-efficient structured cards. Leave a block empty if you don't use that source — the skill will skip it silently.

### Google Drive
- **MCP Server (or local mount path)**: `<mcp-server-name | /path/to/rclone/mount>`
- **Folder URLs to scan**: `<one or more Drive folder URLs, comma-separated>`

### Zoom
- **MCP Server**: `<mcp-server-name>`
- **Account email**: `<email>`
- **Folder/topic filter (optional)**: `<string-or-empty>`

### Notetakers (Fathom / Timeless / Otter / Fireflies / Granola / Gong)
Repeat this block for each notetaker in use:
- **Service**: _Fathom | Timeless | Otter | Fireflies | Granola | Gong_
- **MCP Server (or local export folder)**: `<mcp-server-name | /path/to/exports>`
- **Workspace URL (optional)**: `<paste link>`

### Gmail labels (optional)
- **MCP Server**: `gmail-personal` _(or another configured gmail MCP)_
- **Labels to scan**: `<one or more label names, comma-separated>`

### Local Inbox
- **Path**: `Knowledge/_inbox/` _(always — drop .md / .vtt / .txt / .docx / .pdf files here for ad-hoc ingest)_
